# Assets 资源文件目录

本目录存放 Skill 相关的视觉资源文件。

---

## 文件说明

### csv-example.png

**描述**：生成的 CSV 文件在 Excel/WPS 中打开的示例截图。

**内容**：
- 展示 `_TestCases.csv` 在表格软件中的显示效果
- 包含至少 20 行测试用例示例数据
- 中文正常显示，无乱码
- 字段完整（10 个字段）

**建议尺寸**：1200 x 800 px

---

### workflow.png

**描述**：Skill 完整工作流程图。

**内容**：

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  扫描仓库 ──→ 读取历史 ──→ 扫描PR ──→ 解析Diff             │
│      ↓                                            ↓         │
│  识别新增功能 ←── LLM理解业务 ←── 提取变更信息              │
│      ↓                                                      │
│  去重检测 ──→ 生成CSV ──→ 编号递增 ──→ 导出CSV             │
│      ↓                                                      │
│  更新History ──→ 完成                                       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**建议尺寸**：1600 x 900 px

---

### architecture.png

**描述**：Skill 模块架构图。

**内容**：展示各模块之间的依赖关系：

```
                   ┌─────────────────────┐
                   │   scan_repository   │  (主入口)
                   └────────┬────────────┘
                            │
          ┌─────────────────┼─────────────────┐
          ↓                 ↓                  ↓
┌─────────────────┐ ┌──────────────┐ ┌──────────────────┐
│   parse_diff    │ │ generate_    │ │ duplicate_checker │
│   (解析Diff)    │ │ testcase     │ │ (去重检测)        │
└────────┬────────┘ └──────┬───────┘ └────────┬─────────┘
         │                 │                   │
         └─────────────────┼───────────────────┘
                           ↓
              ┌──────────────────────┐
              │   testcase_id_       │
              │   manager (编号管理)  │
              └──────────┬───────────┘
                         ↓
              ┌──────────────────────┐
              │    merge_csv         │
              │    (CSV合并)         │
              └──────────┬───────────┘
                         ↓
              ┌──────────────────────┐
              │    export_csv        │
              │    (导出CSV)         │
              └──────────────────────┘
```

**建议尺寸**：1200 x 900 px

---

## 资源生成方式

这些图片资源可以通过以下方式生成：

1. **手动截图**：运行 Skill 后截取实际效果
2. **Mermaid 渲染**：使用 Mermaid Live Editor 渲染流程图
3. **AI 生成**：使用 AI 图片生成工具创建

## 当前状态

- [ ] csv-example.png - 待创建
- [ ] workflow.png - 待创建
- [ ] architecture.png - 待创建
